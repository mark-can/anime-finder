{
  "name": "anime-finder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Find the best anime that aired in a selected year using the AniList API.",
  "scripts": {
    "check": "node --check src/app.js && node --check src/anilist.js && node --check src/domain.js && node --check src/i18n.js && node --check src/state.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
